name: Linter

on:
  pull_request:

jobs:
  helm:
   runs-on: ubuntu-latest
   steps:
     - name: Checkout
       uses: actions/checkout@v3

     - name: Setup Helm
       uses: azure/setup-helm@v3
       with:
          version: 3.9.3

     - name: Helm lint
       run: helm lint
